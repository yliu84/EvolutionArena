# Goal 5 商业品质闸门

状态：进行中。地图环境由独立工作流负责，本 Goal 当前不修改地图素材或环境渲染。

正式入口：

`http://127.0.0.1:5178/?maplab=5&debug=1&evolutionSeed=goal5-acceptance`

## 已完成：情境式新手引导

- 真实移动后从“控制身体”切换到“接近窝点”，显示目标距离。
- 第一波出现后解释自动锁定、Tab切换与同一单键三段普攻。
- 明确攻击距离、存活锁定目标与8度接触角规则。
- 首次出手后解释 Biomass 开启进化、Genes 改变候选概率，而不是固定掉落一种皮肤。
- 根据当前锁定的 Fang / Shell / Swarm 显示不同弱点。
- 进化阶段解释三选一、能力代价与一次抗拒。
- Guardian 明确要求绕后；Boss 阶段1/2根据 live pattern 显示三种安全区。
- 胜利与失败都有下一步说明；所有文字只读取权威状态，不计算伤害、进化或胜负。

当前证据：70个测试文件、367项测试；TypeScript、`git diff --check`与Vite生产构建通过。桌面真实W/Space输入完成引导切换和一次有效伤害；Boss调试入口确认实时招式说明；Chrome 844×390确认紧凑HUD、指引、连续摇杆和右侧战斗操作互不遮挡。

## 已完成：声音、反馈设置与暂停基础

- 程序化Web Audio总线覆盖脚步、四种攻击起手、轻/重命中、击杀、玩家受击、进化开启/选择、Boss变阶段、胜利和失败；小/中/大事件拥有不同音量、时长和音高。
- 攻击起手声从动作开始事件触发，命中/击杀声只从权威接触结果触发；声音永远不决定伤害。
- MapLab 5复用本机设置键，支持镜头震动开关、受击闪光开关和60%→100%→静音循环，刷新后保留。
- Esc或HUD“体验设置”打开暂停面板；暂停期间玩家、敌人和正式单局计时全部冻结，继续后恢复。
- Web Audio不可用或被浏览器阻止时不会中断游戏。
- Chrome 844×390设置面板全部按钮、说明和继续操作位于安全区内。

## 已完成：输入重绑、加载恢复与运行预算

- 移动、锁定、普攻和暂停共七项键位可在暂停面板内重绑；冲突键自动交换，支持恢复默认，刷新后保留。
- MapLab 5与旧Phaser入口已分离按需加载：启动入口约4.09 kB，加入手机控制后的MapLab 5逻辑块约117.00 kB；旧入口约1.58 MB但不会随MapLab 5首屏下载。
- 显式加载状态覆盖模块、环境和当前角色；任何启动失败均进入可读错误页，重试会清除QA故障参数并重新启动。浏览器已从模拟失败恢复到`modelReady=true`。
- 三代角色只加载当前阶段GLB。独立运行版保留相同网格、骨骼和动作，仅优化内嵌贴图：初始型6,975,984→2,960,544字节（-57.6%），一级进化8,332,284→1,411,212字节（-83.1%），二级进化8,495,960→4,057,508字节（-52.2%）。三份均为0 glTF错误/0警告；母文件原样保留。
- 正式构建只复制三份运行版与stage 3/6正式模型，排除未使用的建模候选；`dist`由约177 MB降至92 MB。其余体积主要属于冻结中的地图/旧试玩资产，本Goal不改动。
- 调试状态持续记录FPS、平均/P95帧时、draw calls、场景三角、纹理、几何体、视口/DPR及浏览器可用时的JS堆。生产预览桌面1280×720、DPR 1.65样本为119.8 FPS、P95 8.7 ms、218 draw calls、26.4 MB JS heap、首次就绪321 ms；生产预览844×390样本为120 FPS、P95 8.5–8.6 ms、236 draw calls、47.6–51 MB JS heap。两种视口均无console warning/error。
- 生产页实际观察到6个MapLab 5脚本块和当前stage 0运行GLB；`legacy-main`未进入资源清单。844×390下暂停页和七项输入重绑页完整可见，视口覆盖在验证后已复原。
- `debug=1`暂停页直接显示真机证据行：FPS、P95帧时、视口/DPR、draw calls、三角数与浏览器可用时的JS heap。844×390正式包实测读数完整显示且无溢出，手机验收无需远程开发者工具。
- 压缩后stage 0/1/2均在浏览器成功加载；一级进化实际攻击仍造成46→30权威伤害，证明动作与接触链未被贴图优化破坏。

## 已完成：手机横屏布局与双拇指操作

- 手机横屏默认只保留生命、当前目标、资源和两个入口；详细键位与状态可由“展开信息”主动展开，避免永久遮挡战场。
- 左侧四向按钮替换为118px连续虚拟摇杆，支持任意角度、死区、最大行程和力度缩放；松手立即归零。键盘和点击移动仍保持原有行为，所有入口继续共用“先转身再移动”的权威门槛。
- 右侧保留独立锁定键，并把普攻改成“按住连招”：持续按住会依次触发当前形态的单键普攻链，不要求反复点按，也不增加技能系统。
- 竖屏进入时先显示横屏提示；“进入全屏横屏”会在浏览器允许时请求全屏与横屏锁定，失败时给出关闭系统竖屏锁定并手动旋转的说明，同时保留“暂时以竖屏继续”兼容入口。
- 844×390生产预览中，摇杆拖动使角色连续移动并推进权威引导，松手后摇杆归中；HUD展开/收起和竖屏390×844拦截/继续均已实测。浏览器console warning/error为0。

## 待完成

- 真实横屏中端设备帧率、内存和双拇指操作记录（844×390浏览器安全区与连续摇杆已通过，但不冒充真机）。
- 无讲解外部试玩：能否完成整局并解释猎食如何影响随机进化。
- 高频问题记录、修复结论和最终用户验收。

## 已知非阻断项

- Vite仍对旧Phaser试玩的独立legacy块给出大于500 kB提示；MapLab 5首屏已完成拆分，不下载该块。旧试玩继续拆分不应阻塞当前商业切片，也不能误报成MapLab 5首包问题。
- 844×390现阶段为本机Chrome横屏视口，不等于真实中端手机性能证据。
