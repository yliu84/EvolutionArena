# 开放地图 · 整体平面图方案（四选一）

日期：2026-08-18
用途：给用户选一套作为开放地图的拓扑基础。选定后再定区域范围、窝点候选位与过渡点坐标。

四张图都基于同一套既有资产与规则，只有**拓扑**不同：

- 8 个 MapLab 4 窝点原型，按核心血量分三层（18–20 / 22–26 / 28–30）
- 一局穿过三个区域，每区清 2 个窝点
- 窝点从**同层候选位中随机抽取**，所以两局路线不同
- 区域之间的过渡点就是区域首领所在处
- 五个变异里程碑 = 进入区域 ×3 + 区域首领 ×2

| 方案 | 文件 | 深度可读性 | 探索感 | 玩家路线选择 | 美术成本 | 与里程碑闸门 |
| --- | --- | --- | --- | --- | --- | --- |
| **A 同心三环** | `layout-a-concentric-rings.svg` | 高 | 低 | 无 | 低 | 契合 |
| **B 中枢三叶** | `layout-b-hub-and-lobes.svg` | **低** | 中 | **高** | 中 | **冲突** |
| **C 河谷长带** | `layout-c-linear-valley.svg` | **最高** | 最低 | 无 | **最低** | **最高** |
| **D 群岛** | `layout-d-archipelago.svg` | 高 | **最高** | 无 | **最高** | 契合 |

## 关键取舍

**B 的路线选择权和「越往后越难」直接矛盾。** 玩家能任意顺序进三叶，深度就不再是难度。要救它只有两条路：给叶排死难度（等于取消选择权），或按进入顺序动态缩放敌人强度（实现复杂，而且玩家会察觉数值在变）。

**C 是唯一「隘口天然就是首领位」的方案。** 其余三套都要额外解释「为什么这里过不去」。C 的代价是最不像开放世界，更像三段关卡。

**D 的视觉辨识度最高，代价是三倍的地貌美术量**，而且渡口是纯移动时间——如果渡口里没有内容，那段路就是空的。

**A 几何最规整，但外环周长最长、危险最低**：玩家会在最无聊的地方走最多的路。可以靠把外环做窄来缓解。

## 我的建议

**先做 C，把它当作开放地图的第一版骨架。**

不是因为它最好，而是因为它**最容易验证循环是否成立**：深度绝对可读、节奏最容易控制、美术只需三段。等 5 次变异 + 3 条命 + 区域难度这套循环被证明能留住玩家，再把骨架换成 D——**C 到 D 是同一套逻辑换一层地貌，而 C 到 B 要改闸门**。

反过来先做 D，等于同时押注拓扑和循环；循环不成立时，三张岛的美术就白做了。
