{
  "name": "evolutionarenalite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "build:itch": "DEPLOY_BASE=./ npm run build",
    "verify:assets": "node scripts/verify-runtime-assets.mjs --dist",
    "package:itch": "node scripts/package-itch.mjs",
    "preview": "vite preview --host",
    "test": "vitest run",
    "validate:gltf": "node scripts/validate-glb.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "gltf-validator": "^2.0.0-dev.3.10",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "three": "^0.185.1"
  }
}
